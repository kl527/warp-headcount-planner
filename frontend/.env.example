# Base URL of the Cloudflare Worker backend.
# Inlined at build time by Vite — must be set in Vercel's Environment Variables
# for Production and Preview environments.
VITE_BACKEND_URL=https://warp-headcount-planner-backend.leed4219.workers.dev
